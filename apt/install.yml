# Execute this playbook with -e "package=name" to install the package called name.
- name: Install a package
  hosts: all
  become: yes
  tasks:
  - apt:
      update_cache: yes
      name: '{{ package }}'
